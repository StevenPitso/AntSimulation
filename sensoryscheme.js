

class SensorScheme{

    constructor(items){
        this.x;
        this.y
        this.sensors = [
            new Sensor(this.x, this.y, 0, 30,"UEN", 12),
            new Sensor(this.x, this.y, 0, 10, "HJ", 10)
        ];
    }
    update(){

    }
    
}
